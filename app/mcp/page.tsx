export default function Mcp(){ return (<div style={{padding:"30px 0"}}><h1>MCP Connect</h1><p className="muted">Expose this web as Remote MCP to OpenCode.</p><div className="grid g2"><div className="card"><h3>opencode.json</h3><pre>{`{\n  "$schema": "https://opencode.ai/config.json",\n  "mcp": {\n    "codebridge": {\n      "type": "remote",\n      "url": "https://your-domain.com/api/mcp",\n      "headers": { "Authorization": "Bearer PASTE_MCP_KEY" }\n    }\n  }\n}`}</pre><p className="muted small">Get key from /dashboard/mcp after login.</p></div><div className="card"><h3>Tools</h3><ul className="small"><li><code>list_repos</code> — list connected repos</li><li><code>push_code</code> — transport files to GitHub</li><li><code>trigger_build</code> — dispatch Actions workflow</li><li><code>get_build_status</code> / <code>get_build_logs</code></li></ul></div></div></div>); }
