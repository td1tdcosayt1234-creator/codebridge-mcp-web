export default function McpControl(){ return (<div className="card"><h3>MCP Control</h3><p className="muted small">Server: /api/mcp (remote). Tools: push_code, trigger_build, get_build_status, get_build_logs, list_repos.</p><pre>tools: push_code=true, trigger_build=true, logs=true</pre><p className="muted small">Disable per-agent via config: tools: codebridge_* = false then enable per agent.</p></div>); }
