export default function McpControl(){ return (<div className="card"><h3>MCP Control</h3><p className="muted small">Server: /api/mcp (remote, Streamable HTTP). Exactly 2 tools: compile (all types) + compile_fix (AI self-fix). Each call submits, waits ~45s and returns the live result. No headers needed — identity resolves from optional key, login cookie, or shared guest pool (refilled daily).</p><pre>tools: compile=true, compile_fix=true</pre><p className="muted small">Disable per-agent via config: tools: codebridge_* = false then enable per agent. Guest pool usage shows as mcp_guest in /admin/tokens.</p></div>); }
