export default function McpControl(){ return (<div className="card"><h3>MCP Control</h3><p className="muted small">Server: /api/mcp (remote, Streamable HTTP). Signup/login required — every call must carry a registered user identity (personal key header or login cookie); anonymous calls are rejected. Exactly 2 tools: compile (all types) + compile_fix (AI self-fix). Each call submits, waits ~45s and returns the live result.</p><pre>tools: compile=true, compile_fix=true, auth=required</pre><p className="muted small">Disable per-agent via config: tools: codebridge_* = false then enable per agent. Per-user usage shows in /admin/tokens.</p></div>); }
