export default function McpControl(){ return (<div className="card"><h3>MCP Control</h3><p className="muted small">Server: /api/mcp (remote, Streamable HTTP). Signup/login required — every call must carry a registered user identity (personal key header or login cookie); anonymous calls are rejected. 6 tools: compile, compile_fix, list_tasks, get_task_result, auth_check, gh_issue_list. Each compile submits, waits and returns the live result.</p><pre>tools: compile=true, compile_fix=true, list_tasks=true, get_task_result=true, auth_check=true, gh_issue_list=true, auth=required</pre><p className="muted small">Disable per-agent via config: tools: codebridge_* = false then enable per agent. Per-user usage shows in /admin/tokens.</p></div>); }
