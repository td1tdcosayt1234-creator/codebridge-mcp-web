export default function McpControl(){ return (<div className="card"><h3>MCP Control</h3><p className="muted small">Server: /api/mcp (remote, Streamable HTTP). 2 tools: run_task (compile/fix request, coin hold) + get_task_result. No headers needed — identity resolves from optional key, login cookie, or shared guest pool (refilled daily).</p><pre>tools: run_task=true, get_task_result=true</pre><p className="muted small">Disable per-agent via config: tools: codebridge_* = false then enable per agent. Guest pool usage shows as mcp_guest in /admin/tokens.</p></div>); }
