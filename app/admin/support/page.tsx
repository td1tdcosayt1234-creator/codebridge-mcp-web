"use client"; import { useEffect, useState } from "react";
export default function AdminSupport(){ const [d,setD]=useState<any>(null); useEffect(()=>{fetch("/api/admin/overview").then(r=>r.json()).then(setD);},[]); if(!d) return <p>Loading</p>; if(d.error) return <p>Admin only</p>; return (<div className="card"><h3>Support tickets</h3>{(d.tickets||[]).map((t:any)=>(<div key={t.id} style={{borderBottom:"1px solid #ffffff12",padding:"8px 0"}}><b>{t.subject}</b> <span className="badge">{t.status}</span><div className="small muted">{t.body} — {t.at}</div></div>))||<p className="muted">no tickets</p>}</div>); }
