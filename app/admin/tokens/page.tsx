"use client"; import { useEffect, useState } from "react";
export default function AdminTokens(){ const [d,setD]=useState<any>(null); useEffect(()=>{fetch("/api/admin/overview").then(r=>r.json()).then(setD);},[]); if(!d) return <p>Loading</p>; if(d.error) return <p>Admin only</p>; const emailOf=(id:string)=>(d.users||[]).find((u:any)=>u.id===id)?.email||id; return (<div className="card"><h3>Token uses (all users)</h3><table><thead><tr><th>User</th><th>MCP</th><th>GitHub</th><th>Balance</th><th>Used</th></tr></thead><tbody>{(d.usage||[]).map((u:any)=>(<tr key={u.userId}><td className="small">{emailOf(u.userId)}</td><td>{u.mcpCalls}</td><td>{u.githubCalls}</td><td>{u.balance??0}</td><td>{u.usedTotal??0}</td></tr>))}</tbody></table></div>); }
