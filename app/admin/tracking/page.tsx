"use client"; import { useEffect, useState } from "react";
export default function AdminTracking(){ const [d,setD]=useState<any>(null); useEffect(()=>{fetch("/api/admin/overview").then(r=>r.json()).then(setD);},[]); if(!d) return <p>Loading</p>; if(d.error) return <p>Admin only</p>; return (<div className="card"><h3>Global tracking</h3><table><thead><tr><th>Time</th><th>User</th><th>Action</th><th>Detail</th></tr></thead><tbody>{(d.events||[]).map((e:any)=>(<tr key={e.id}><td className="small">{e.at}</td><td className="small">{e.userId}</td><td><span className="badge">{e.action}</span></td><td className="small">{e.detail}</td></tr>))}</tbody></table></div>); }
