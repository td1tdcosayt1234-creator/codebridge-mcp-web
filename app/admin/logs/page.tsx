"use client"; import { useEffect, useState } from "react";
export default function Logs(){ const [d,setD]=useState<any>(null); useEffect(()=>{fetch("/api/admin/overview").then(r=>r.json()).then(setD);},[]); if(!d) return <p>Loading</p>; if(d.error) return <p>Admin only</p>; const logs=(d.events||[]).filter((e:any)=>["login","login_fail","lockout","vpn_block","ad_earn","admin_github_token_save","admin_github_token_remove","runner_token_regen"].includes(e.action)); return (<div className="card"><h3>Security logs</h3><table><thead><tr><th>Time</th><th>Action</th><th>Detail</th></tr></thead><tbody>{logs.map((e:any)=>(<tr key={e.id}><td className="small">{e.at}</td><td>{e.action}</td><td className="small">{e.detail}</td></tr>))}</tbody></table></div>); }
