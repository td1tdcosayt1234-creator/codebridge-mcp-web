export default function About(){ return (<div style={{padding:"30px 0"}}><h1>About</h1><p className="muted">CodeBridge connects OpenCode coding agent to GitHub via MCP. Transport code, trigger Actions, track builds, manage tokens.</p><div className="grid g2"><div className="card"><h3>Why</h3><p className="muted small">Manual copy-paste is slow. Agent should push directly, CI should build automatically.</p></div><div className="card"><h3>How</h3><p className="muted small">Remote MCP server + Classic Token vault + Octokit + Actions dispatch + dashboard tracking.</p></div></div></div>); }
