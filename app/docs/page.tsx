export default function Docs(){ return (<div style={{padding:"30px 0"}}><h1>Docs</h1><div className="grid g2"><div className="card"><h3>1. Create Classic Token</h3><p className="muted small">GitHub → Settings → Developer settings → Personal access tokens (classic) → Generate → scopes: repo, workflow. Copy ghp_...</p></div><div className="card"><h3>2. Add to dashboard</h3><p className="muted small">/dashboard/github → paste token → Save → Test. Token is AES-256-GCM encrypted, never shown full.</p></div><div className="card"><h3>3. Connect OpenCode</h3><pre>{`{\n  "mcp": {\n    "codebridge": {\n      "type": "remote",\n      "url": "https://your-domain.com/api/mcp",\n      "headers": { "Authorization": "Bearer YOUR_MCP_KEY" }\n    }\n  }\n}`}</pre></div><div className="card"><h3>4. Agent flow</h3><pre>push_code(files, repo, branch) → trigger_build → get_build_status → get_build_logs</pre><p className="muted small">Sample workflow .github/workflows/build.yml: checkout → setup-node → npm ci → npm run build.</p></div></div></div>); }
